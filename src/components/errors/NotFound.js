import { Typography } from "@material-ui/core";

const NotFound = ()=>{
    return (
        <Typography 
            variant="h5"
            style={{fontWeight:'bold'}}
        >
            Not Found Page!
        </Typography>
    );
};

export default NotFound;